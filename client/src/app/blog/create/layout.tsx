import React from "react";
import Image from "next/image";

const CreateBlogLayout = ({children}: { children: React.ReactNode }) => {
    return (
        <div>
            {children}
        </div>
    )
}
export default CreateBlogLayout